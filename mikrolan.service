[Unit]
Description=MikroLan Backend API
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/mikrolan/src
EnvironmentFile=/etc/mikrolan.env
ExecStart=/usr/local/bin/uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
