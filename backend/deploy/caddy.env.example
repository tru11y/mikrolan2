# Copier en caddy.env sur le VPS (jamais commité), remplir avec un IAM user
# dédié, scope minimal : route53:ChangeResourceRecordSets + route53:GetChange
# + route53:ListHostedZonesByName sur la zone mikrolan.net uniquement.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_HOSTED_ZONE_ID=
